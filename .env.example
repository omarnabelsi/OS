# Optional: SteamGridDB API key (https://www.steamgriddb.com/profile/preferences/api)
# Also settable at runtime in Settings > Artwork. Without it, artwork falls back to the Steam CDN.
AURA_STEAMGRIDDB_API_KEY=
